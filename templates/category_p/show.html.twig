{% extends 'admin/baseAdmin.html.twig' %}
{% block title %}CategoryP{% endblock %}

{% block body %}
    <h1>Categories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_p.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_p.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_p.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_p_index') }}">back to list</a>

    <a href="{{ path('app_category_p_edit', {'id': category_p.id}) }}">edit</a>

    {{ include('category_p/_delete_form.html.twig') }}
{% endblock %}
